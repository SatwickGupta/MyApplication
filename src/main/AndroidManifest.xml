<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".RetrofitActivity3"></activity>
        <activity
            android:name=".RetrofitActivity2"
            android:label="@string/title_activity_retrofit2"
            android:theme="@style/Theme.MyApplication.NoActionBar" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".FragmentDemoActivity" />
        <activity android:name=".CardViewActivity" />
        <activity android:name=".RetrofitActivity" />
        <activity android:name=".GridLayoutActivity" />
        <activity android:name=".AudioActivity" />
        <activity android:name=".VideoViewActivity" />
        <activity android:name=".ProgrammingRecyclerview" />
        <activity android:name=".IntentActivity" />
        <activity android:name=".OTPActivity" />
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>